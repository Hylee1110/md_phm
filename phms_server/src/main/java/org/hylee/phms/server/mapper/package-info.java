/**
 * MyBatis Mapper 接口。
 * <p>
 * 与 {@code resources/mapper/*.xml} 一一对应，负责数据库访问。
 * 建议：
 * <ul>
 *   <li>仅做数据访问，不在 mapper 中实现业务规则</li>
 *   <li>返回值/入参尽量使用明确的 DO/基础类型，便于维护 SQL 映射</li>
 * </ul>
 */
package org.hylee.phms.server.mapper;

