/**
 * `phms_pojo`：跨模块共享的领域模型（Model）包。
 * <p>
 * 与后端的 DO/DTO/VO 区分：
 * <ul>
 *   <li>Model：领域/业务模型，跨模块共享</li>
 *   <li>DO：数据库持久化对象（字段与表结构强相关）</li>
 *   <li>DTO：请求入参对象（携带校验规则）</li>
 *   <li>VO：响应展示对象（贴合页面需要）</li>
 * </ul>
 */
package org.hylee.phms.pojo.model;

